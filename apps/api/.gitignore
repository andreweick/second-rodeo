# Cloudflare Workers API
#
# Most ignore patterns are defined in the root .gitignore
# This file only contains patterns specific to this Worker project

# Worker-specific local state (already covered by root **/.wrangler/ pattern)
# Kept here for clarity when working in this directory
.wrangler/

# Local development secrets (already covered by root .dev.vars pattern)
# Kept here as documentation for developers
.dev.vars
.dev.vars.*
!.dev.vars.example
